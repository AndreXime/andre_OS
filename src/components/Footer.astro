---
import { formatDate } from "@/lib/utils";

interface Props {
    lastDate: string | undefined;
}

const { lastDate } = Astro.props;
---

<footer
    class="fixed bottom-0 w-full bg-zinc-950 border-t border-zinc-900 py-2 px-6 flex justify-between items-center text-[10px] md:text-xs font-mono text-zinc-500 z-40"
>
    <div class="flex items-center gap-4">
        <span class="flex items-center gap-1.5"
            ><span class="w-1.5 h-1.5 rounded-full bg-emerald-500/50"></span> System:
            Online</span
        >
        <span class="hidden md:inline text-zinc-700">|</span>
        <span class="hidden md:inline"
            >Last Update: {
                lastDate ? formatDate(new Date(lastDate)) : "00:00"
            }</span
        >
    </div>
    <div class="flex items-center gap-4">
        <span>© {new Date().getFullYear()} Andre Dev</span>
    </div>
</footer>
